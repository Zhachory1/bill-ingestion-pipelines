# Implementation Plan: Fix Issues #9, #10, #11

## Issues Overview

### Issue #9: Fix daily ingest checkpoint tracking
**Priority:** HIGH - Data reliability issue
**Scope:** Backend ingestion pipeline
- Replace unreliable `HEAD@{1}` reflog syntax with stored commit SHA
- Use existing `IngestCheckpoint` model to persist last processed commit
- Update checkpoint only after successful processing

### Issue #10: Validate LLM provider/model combinations
**Priority:** HIGH - Configuration bug causing runtime failures
**Scope:** Configuration and LLM client
- Add enum validation for LLM_PROVIDER (anthropic/openai)
- Use provider-specific default models
- Fail fast for invalid configurations
- Add comprehensive tests

### Issue #11: Add rate limiting for expensive endpoints
**Priority:** HIGH - Security/cost control
**Scope:** API endpoints (search, chat, fulltext)
- Add per-IP rate limiting using slowapi
- Bound query/message lengths
- Return proper HTTP error codes (429, 413)
- Make limits configurable

## Implementation Strategy

### Phase 1: Issue #10 (LLM Provider Validation) - ~30 min
**Files to modify:**
- `app/config.py` - Add enum for providers, provider-specific defaults
- `app/chat/llm.py` - Use validated provider enum
- `tests/chat/test_llm.py` - Add validation tests
- `README.md` - Document provider-specific models

**Changes:**
1. Create `LLMProvider` enum with `ANTHROPIC` and `OPENAI`
2. Add provider-specific model defaults dict
3. Validate provider in settings with Pydantic validator
4. Update LLMClient to use validated provider
5. Add tests for both providers and invalid config

### Phase 2: Issue #9 (Checkpoint Tracking) - ~45 min
**Files to modify:**
- `app/ingestion/daily_dl.py` - Replace HEAD@{1} with checkpoint lookup
- `app/db/models.py` - May need checkpoint helper methods
- `tests/ingestion/test_daily_dl.py` - Add checkpoint tests

**Changes:**
1. Add checkpoint type constant for daily ingestion
2. Query checkpoint table for last commit SHA
3. Use stored SHA for git diff instead of HEAD@{1}
4. Update checkpoint after successful processing
5. Handle first-run case (no checkpoint exists)
6. Add tests for first run, incremental, invalid SHA

### Phase 3: Issue #11 (Rate Limiting) - ~60 min
**Files to modify:**
- `app/config.py` - Add rate limit settings
- `app/main.py` - Add slowapi middleware
- `app/api/search.py` - Add rate limits
- `app/api/chat.py` - Add rate limits and input bounds
- `app/api/bills.py` - Add rate limits
- `app/api/schemas.py` - Add max length validators
- `tests/api/test_rate_limiting.py` - New test file
- `README.md` - Document rate limit configuration

**Changes:**
1. Add slowapi dependency (if not present)
2. Configure per-IP rate limits in settings
3. Add rate limit decorators to expensive endpoints
4. Add query/message length validators in schemas
5. Test rate limit enforcement with multiple requests
6. Document how to disable for local dev

## Testing Strategy

- **Unit tests**: Each change gets unit tests
- **Integration tests**: Test end-to-end flows with new validations
- **Manual testing**: Run local server and verify behaviors

## Success Criteria

- All existing tests pass
- New tests cover edge cases
- README updated with configuration examples
- PR includes closes #9, closes #10, closes #11
- Code review findings addressed
- No breaking changes to existing functionality
